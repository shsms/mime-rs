#+title: mime-rs — pending work
#+startup: logdrawer

* Engine

** TODO Shrink upd-epub-to-jp.tl's packed error message
Failure JSONs now carry the failed run's ~reports~/~log~ across all three
front-ends, so the script can go back to a plain ~(error ...)~ with the
diagnostics living in reports/log again.

** TODO ulysses scripts: drop the set-buffer-home workaround
The closing report now tracks the primary buffer, so add-anno /
place-diff / upd-epub-to-jp on ulysses-annotated's ~mime-rs~ branch can
drop their end-of-run ~set-buffer~ dance.

** TODO Streaming search [#C]
DONE so far: literal ~search_forward~ streams the range chunk-at-a-time on
a ~memchr::memmem~ finder (SIMD two-way), stopping at the first hit;
literal ~search_backward~ streams 16K-char chunks DOWNWARD from point with
a needle-sized overlap (was: materialize the whole [bound, point) window);
~re_search_forward~ / ~re_search_backward~ / ~looking_at~ are adaptively
windowed. Remaining: a resumable streaming DFA (~regex-automata~) to close
the residual — a regex MISS still grows the window to the bound (the
engine must see every byte to say "no"); and global-replace a multi-GB
file across cores.
/Exit: global-replace a multi-GB file across cores; ulysses bench beats
legacy ~mime~./

** TODO Paged reader holds an open fd per buffer [#C]
DECIDED (2026-06): a drifted paged buffer must NOT re-open the path
mid-life — swapping inodes under a live piece tree mixes old and new
bytes, and the old inode IS the correctness anchor for a modified
buffer's edits. The exposure is mitigated at the session layer instead:
auto-revert re-opens clean drifted buffers on next use, the warm-session
cap (clean-LRU eviction) bounds held fds, and ~close_session~ releases
deliberately — together these also bound the ~.nfsXXXX~ silly-rename
lingering on NFS (now gitignored). Remaining idea, if fd pressure ever
shows up in practice: a re-open-on-demand mode for CLEAN buffers only.

** TODO Dogfooding friction: MCP "insert relative to a defun/node" [#C]
From driving mime to edit mime-rs itself. DONE: the unsaved state is now
legible — ~session_status~ carries an ~unsaved~ flag per session and
~run_program~/~replace_text~/~insert_text~ flag/note an edit that lives only in
the warm buffer (file-backed + modified), so a forgotten ~save~ can't vanish
silently. Remaining: appending a large NEW block has no anchor —
~treesit-insert-sibling~ covers it in lisp, but an MCP-level "insert relative to
a defun/node" convenience (or a ~save~-by-default flag) would close the common
"add a function after X" case without hand-writing a ~run_program~. Possible
further step: a warm buffer with unsaved edits AND a drifted file is the genuine
conflict — could surface it more loudly than the per-tool flag.

** TODO Bytecode-compile hot programs [#C]

** TODO Dirty-span diffs [#C]

** TODO Span-bounded search (anchors past an explicit BOUND)
With an explicit search BOUND, ~$~/~\b~ treat the haystack cut as a
boundary where Emacs consults the real buffer (it backtracks the match
to fit the bound AND evaluates assertions against the text past it).
The high-level ~regex~ API can't express both; ~regex-automata~'s
~Input::span~ can — matches confined to a span, look-around reading
outside it. Switch the searches to it and the divergence (documented on
~Buffer::re_search_forward~) closes.

** TODO Per-program resource limits
A wall-clock/CPU bound per program — blocked on tulisp cooperative
cancellation (eval interruption), see the wishlist below.

* MCP / agent UX

The agent-facing tool surface. Recurring lens: conveniences that
matter MOST for less capable models — defaults that make wrong edits
an error instead of a silent outcome, one-call recovery, and
discoverability over memorized conventions. The 2026-06 review batch
(uniform path addressing, undo_last, expect_unique, close_session,
outline + scope/anchor, help + describe-mcp, diff clamping, search
direction/echo/case-folding, viewport echo) has landed, as has the
follow-up round (multi-file ~files:~ batches, clean-LRU session cap,
decorated defun spans, ~make docs~ generation); what follows is the
remainder.

** TODO Cross-file search: a grep tool (or occur over files/glob) [#B] :dogfooding:

The biggest reason the agent still shells out to =grep -rn= / =sed -n=
mid-edit: =occur= is per-buffer, so "which files mention X" and "show
me that region in three files" have no MCP answer — every multi-file
question drops to Bash, then comes BACK to mime for the edit. The
sandbox already confines paths to =$MIME_ROOTS=, so a read-only
=grep {pattern, files | glob, mode, nlines}= tool (occur's output
shape, one header per file, hit cap) fits the tier. Bonus: its file
list feeds =replace_text {files: …}= directly — search and cross-file
edit would share one vocabulary. A =read_region {path, start_line,
end_line}= alias for "lines 120–140 of X" already exists; the gap is
purely the multi-file fan-out.

** TODO save_buffer {to:} rebinds the visited file — a footgun for path-keyed sessions [#B] :dogfooding:

Hit during the 2026-06-11 switchyard session. Recovery sequence after a
bad save: ~save_buffer {path: X, to: TMP}~ to park the good buffer, then
~mv TMP X~ — but the save-as had REBOUND the session's visited file to
TMP, so the next ~save: true~ on the same session hit TMP's stale-guard
("deleted on disk") instead of writing X, and the fix was a second
save-as back onto X itself.

Emacs' ~write-file~ does rebind, but mime's sessions are KEYED by
canonical path — a session opened as X that silently starts visiting TMP
breaks the addressing contract (the warm session is still listed under
X, yet saves target TMP). For an agent, save-as is almost always "export
a copy", not "move my session".

Fix: make ~to:~ a pure copy-out (write the bytes, leave the visited file
and stale-guard baseline untouched). If a true rename-the-session
operation is ever wanted, make it explicit (~rebind: true~ or a separate
tool). Also worth a line in ~help {topic: "sessions"}~ either way.

** TODO Consider expect_unique as replace_text's DEFAULT [#B]
The flag exists and errors with the match lines; for weaker models
ambiguity-as-error is arguably the right default. Measure the token
/ ergonomics cost on real sessions before flipping (a default flip
breaks "replace the first TODO" one-shots).

** TODO Pattern translation misses: escaped metachars and alternation :dogfooding:
:LOGBOOK:
- State "TODO"       from              [2026-06-11 Do 23:45]
:END:
From an agent session (2026-06-11) driving git-conflict resolution
and TOML/Rust edits via MCP: ~replace_text~ patterns
with RE2-style escapes (~\[features\]~) and ~occur~ with ~|~
alternation returned "no match" where the literal text (and ~search~
exact mode) matched fine — occur even echoed the translated emacs
regex ~features\|dependencies~ yet found nothing. Agents fall back to
literal patterns, losing regex entirely. Reproduce, fix the RE2→emacs
translation (or document the supported dialect precisely), and add
round-trip tests for escaped metachars + alternation.

** TODO conflict-replace eats the trailing newline               :dogfooding:
:LOGBOOK:
- State "TODO"       from              [2026-06-11 Do 23:45]
:END:
Twice in the same session ~(conflict-replace TEXT N)~ joined the
replacement onto the line following the conflict block
(~mod a;mod b;~) because TEXT lacked a trailing newline.
Replacing a conflict hunk is line-oriented; ensure a final newline on
the replacement (or take lines, not raw text).

** TODO Rebase / cherry-pick driver                                     :git:
:LOGBOOK:
- State "TODO"       from              [2026-06-18 Do 09:27]
:END:

Agent-driven rebase / cherry-pick belongs here, not as a separate MCP
server: the agent-hostile 80% is conflict resolution during replay, which
mime already owns (~conflicts~, ~conflict-resolve-trivial~, ~conflict-keep~,
~conflict-replace~, ~conflict-context~ — conflict.rs + builtins.rs
1742–1965). A clean rebase needs no editing tool; a conflicted one needs
exactly this surface.

*** Approach (decided 2026-06-24): in-process git sequencer on a Rust git library — do NOT shell out to ~git~

Three approaches were weighed:

1. Shell out to ~git rebase -i~ with editor shims (~GIT_SEQUENCE_EDITOR~ /
   ~GIT_EDITOR~ → a ~mime~ sub-process that hosts the todo/message file and
   blocks until the agent resumes). REJECTED. (a) The MCP server is
   stdio-only and holds sessions in-process (~HashMap<String, Workspace>~,
   mcp.rs:76); a separately-spawned shim CANNOT see the agent's warm
   sessions, so the rendezvous degrades to an on-disk scratch file + sentinel
   handshake. (b) Shelling out re-opens every side-channel that makes ~git~
   unsafe for a sandboxed agent — hooks, ~--exec~, gitattributes
   clean/smudge filters, ~core.fsmonitor~, ~ext::~/ssh transports — i.e.
   arbitrary code execution + network. (c) Breaks the "no process spawn"
   invariant (safety.rs:1-10).

2. MCP server spawns the ~git~ binary directly. REJECTED for (b)+(c) above:
   ~git rebase --exec~ and hooks run arbitrary code, so a sandboxed agent
   able to invoke ~git~ has a full sandbox escape.

3. *In-process git "sequencer" on a Rust git library (CHOSEN).* Drive
   rebase / cherry-pick / revert as a sequence of pick → 3-way-merge →
   commit steps that mime owns end to end. This dissolves the ~$EDITOR~
   problem: history manipulation (reorder / squash / reword / drop) is DATA
   the agent passes or edits, never an editor invocation. The only hard
   primitive is a 3-way merge that writes conflict markers into the worktree
   — which routes straight into the existing conflict tools.

Why a library, not the ~git~ binary:
- No process spawn → mime's "no shell, no process spawn, no network"
  invariant (safety.rs:1-10) is PRESERVED. Git becomes in-process Rust calls,
  like the existing file ops.
- No shell-out side-channels. ~git~ is dangerous in a sandbox not for its
  data ops but for hooks / ~--exec~ / clean+smudge filters / fsmonitor /
  pager / editor / ~ext::~ transports. A library has none of these unless we
  wire them — "git minus code-execution" is the DEFAULT, not something we
  fight the binary to suppress.
- Network blocked by OMISSION, not by filtering flags. fetch / push / clone /
  remote are a SEPARATE API surface; the sequencer never calls or exposes
  them. Defense in depth: reject any op needing a remote; never register
  remote/transport tools. (This is the clean form of "block push/pull/remote"
  — clean because it's a different code path, not a flag on one omnipotent
  binary.)

Security checklist — block the non-network arbitrary-code channels too (they
are subtler than the network ones):
- Hooks: never run ~.git/hooks~. (libgit2 does not run hooks itself; if
  hand-rolled, never invoke them. Bonus: deterministic, no surprise side
  effects.)
- ~--exec~ / arbitrary step commands: do not implement.
- gitattributes process filters (clean/smudge): never register process-filter
  callbacks; built-in ident/crlf are fine.
- ~core.fsmonitor~ / ~core.sshCommand~ / ~core.pager~ / ~core.editor~ /
  aliases / ~http.*~ / ~url.*.insteadOf~: moot once remote/exec/editor paths
  are unexposed, but audit that the chosen lib honors no repo-local config
  key that shells out.
- Confine to repos under MIME_ROOTS: ~safety::check_path~ the repo path,
  refuse repos outside the allowlist (mirrors open_file, mcp.rs:551).

Library: ~git2~ (libgit2 bindings) — DECIDED 2026-06-24. It has complete,
mature merge / cherry-pick / checkout-with-conflict-style TODAY, so the
DISCRIMINATING capability (3-way merge → diff3 markers in the worktree) is
proven, not in-flight — exactly the risk gix carried. Pure-Rust ~gix~ was
considered and DEFERRED: ~gix-merge~ (its conflict-marker merge) is the newer,
less-proven piece; revisit a migration once it is production-grade (the
sequencer's library surface is small, so a later swap is contained).
- C dependency: build libgit2 from source via the ~git2~ ~vendored~ feature
  (no system libgit2 needed; adds a ~cc~ toolchain + build time — fine, the
  VM can install build tools).
- KEY API NUANCE: do NOT use libgit2's high-level ~Repository::rebase~ — it
  replays the commits between upstream and branch as plain picks and does NOT
  express reorder / squash / reword / drop. HAND-ROLL the sequencer over the
  low-level primitives: ~cherrypick_commit~ / ~merge_commits~ → in-memory
  ~Index~; ~checkout_index~ with ~CheckoutBuilder::conflict_style_diff3~ to
  write markers to the worktree; ~index.write_tree~ + ~Repository::commit~ to
  land each step; ref update to move the branch; ~reset(.., Hard)~ to the
  pre-op snapshot for ~git_abort~. (Verify exact signatures against the
  installed ~git2~ version — they drift; knowledge cutoff 2026-01.)
- Hooks: libgit2 does not run ~.git/hooks~ — nothing to suppress, but never
  add hook execution. Do not register custom (process) filters; built-in
  crlf/ident are harmless.
- Smoke-test (DONE 2026-06-24, sequencer.rs): ~cherrypick_commit~ of two
  conflicting commits + ~checkout_index~ diff3 → conflict.rs's scanner parses
  the markers, base section and all (it understands ours/theirs/base + flags
  strays, builtins.rs:1773-1778). git2 ~vendored-libgit2~ builds with just
  ~cc~ (no cmake) in the VM.

*** The sequencer

mime implements git's "sequencer" — replay a list of pick/commit steps with
conflict stops — unified across rebase, cherry-pick, and revert (git unifies
these too: .git/sequencer + CHERRY_PICK_HEAD / REBASE_HEAD).

State lives in ~.git~ (re-entrant + crash-safe, mirrors git), NOT only in the
warm session: each tool call re-opens the repo and reads the in-progress
operation from our own ~.git/mime-sequencer~ state file (the plan is
hand-rolled over git2's cherrypick primitives — see Approach — so libgit2's
own rebase state is unused). So ~rebase~ → ~continue~ → ~continue~ survives a
server restart and never desyncs.

Step loop, per planned commit:
1. 3-way merge (cherry-pick) the commit onto current HEAD;
2. clean → create the commit (planned message for reword/squash), advance,
   recurse;
3. conflicted → write markers to the worktree, RELOAD any warm buffer whose
   file changed (mime is the worktree authority — it knows exactly which
   files moved; reuse ~Quire::rebase_to~, quire.rs:852 / revert), then STOP,
   returning a conflict state.

*** Agent-facing MCP tools (new ~git_*~ group)

History ops (start an operation; each may return a conflict stop):
- ~git_rebase~ {repo, upstream | onto, plan?} — plan = list of {commit,
  action: pick|reword|squash|fixup|drop, message?}; list order = new order.
  Omit plan → pick-all in order. No ~$EDITOR~, no todo buffer; the plan is
  data. (Optional sugar: expose the plan as an editable buffer for users who
  prefer to hand-edit it — not required.)
- ~git_cherry_pick~ {repo, commits, onto?}
- ~git_revert~ {repo, commits}

Sequencer control (polymorphic over the in-progress op, like git's
sequencer — not per-op):
- ~git_continue~ {repo} — after the agent resolves conflicts with the
  existing conflict tools and saves, commit the resolved tree and advance;
  returns next state (another conflict stop, or done). Pre-check: refuse if
  ~conflict-count~ > 0 — a free safety net.
- ~git_skip~ {repo} — drop the current step, advance.
- ~git_abort~ {repo} — restore HEAD/refs/worktree to the pre-op snapshot.
- ~git_status~ {repo} — {operation, current_step, done/remaining steps,
  conflicted_files, head}. The poll/inspect call.

Read/inspect (safe; for building a plan):
- ~git_log~ {repo, range?} — commits (hash, subject, author).
- ~git_show~ {repo, commit} — metadata + diff.

Conflict resolution adds NO new surface — the agent uses the existing
~conflicts~ / ~conflict-context~ / ~conflict-keep~ / ~conflict-replace~ /
~conflict-resolve-trivial~ tools on the conflicted worktree files, saves,
then calls ~git_continue~. Naming: ~git_*~ prefix groups them; verbs mirror
git so the agent's git mental model transfers.

*** Tier / gating

Rewriting history + checkout is more powerful than text editing (a bad reset
destroys uncommitted work). Gate it like ~register_orchestration~
(engine.rs:314): a new ~register_git~ group, registered only on explicit
opt-in (env, e.g. ~MIME_GIT_ROOTS~, or a flag), default OFF, repos confined to
MIME_ROOTS via ~check_path~. When off, the ~git_*~ tools are not registered at
all (same registration-time gating as the sandboxed/trusted split) — the
default sandbox stays byte-for-byte unchanged, and git is an explicit,
bounded, auditable capability, NOT a tier escalation.

*** Interim (works today, zero mime changes)

The agent can already drive ~git rebase~ via Bash and open the conflicted
files in mime to resolve them — that is the stop-gap. The sequencer is the
productized, deterministic, ~$EDITOR~-free version that makes mime the single
worktree authority.

*** Progress (branch ~rebase-sequencer~)

- [X] git2 ~vendored-libgit2~ added (builds with ~cc~ alone, no cmake).
- [X] Smoke test: cherrypick conflict → diff3 worktree markers → conflict.rs
  parses them (~src/sequencer.rs~).
- [X] Sequencer core in ~sequencer.rs~ (~Plan~ / ~Step~ / ~Action~, the
  ~start~ / ~continue_op~ / ~abort~ / ~status~ surface). Step loop:
  ~Repository::cherrypick~ onto a detached, moving HEAD with a diff3 checkout →
  clean: commit & advance / conflicted: stop with markers in the worktree.
  State persisted to ~.git/mime-sequencer.json~ (re-entrant); ~abort~ =
  ~reset --hard~ to the pre-op snapshot; branch ref only moves at ~finish~.
  Pick / reword / drop + conflict→continue + abort covered by unit tests.
- [X] Squash / fixup (~land_step~): re-parent on ~current~'s parent + meld the
  message (squash concatenates, fixup keeps the first); a leading squash/fixup
  is rejected. Cherry-pick (~onto~ = HEAD) and revert (inverse, ~Mode::Revert~)
  share the loop. ~git_skip~ drops the stopped step.
- [X] Worktree authority: handled by mime's EXISTING auto-revert — clean
  drifted buffers re-read on next session use (~auto_revert_if_clean~,
  engine.rs:620, called on the run path mcp.rs:435), modified buffers are
  save-guarded. No proactive reload hook needed, matching the paged-reader
  decision; the sequencer stays a free function over a ~git2::Repository~.
- [X] ~git_*~ MCP tools gated by an opt-in env (~MIME_GIT_ROOTS~, default OFF):
  ~git_rebase~ / ~git_cherry_pick~ / ~git_revert~ / ~git_continue~ / ~git_skip~
  / ~git_abort~ / ~git_status~ / ~git_log~ / ~git_show~. Schemas appended only
  when enabled (so ~tools/list~ + ~validate_args~ omit them when off); dispatch
  rejects ~git_*~ when off; repo path ~check_path~-confined. git2 stays entirely
  in ~sequencer.rs~ via path-facing ~cmd_*~ wrappers. Verified end-to-end
  through the real MCP JSON-RPC server (clean rebase, conflict→continue,
  gating on/off).

DONE — the sequencer is feature-complete and tested (13 sequencer + 2 MCP unit
tests; manual end-to-end). Optional follow-ups, not blocking:
- ~git_show~ shows changed-file status only; full unified diff could be added.
- ~describe-mcp~ documents the default catalogue; regenerate with
  ~MIME_GIT_ROOTS=1~ if the git tools should appear in docs/mcp-tools.md.
- Migrate to pure-Rust ~gix~ once its conflict-marker merge is production-grade
  (the ~cmd_*~/typed API is the small surface to swap).

/Exit: ~git_rebase~ with a squash+reword plan over a 3-commit range, one
conflicted step resolved via the existing conflict tools + ~git_continue~,
yields the same tree as ~git~; ~git_abort~ mid-conflict restores the pre-op
state exactly; no network/exec/hook code path is reachable from any ~git_*~
tool./

* Structural editing

** TODO Incremental re-parse (~InputEdit~s) [#C]
Nodes are first-class values with relational navigation now; the last
piece is feeding ~InputEdit~s from buffer mutations instead of a full
re-parse per edit. Needs edit logging in both stores (byte offsets +
row/col Points at mutation time), lazily enabled on first treesit use
so non-treesit workloads pay nothing.

** TODO More languages [#C]
JS/TS, Go, …: a ~Lang~ variant + extension row + ~defun_kinds~ row each. Fix
Python ~decorated_definition~ spans (include the decorators). Adjacent: a
formatter for code edits (~rustfmt~ via ~call-process~, trusted tier only).

* TUI cockpit (mime tui)

A terminal UI over the engine — an observer/stepper first, an editor only
if daily use pulls it there. Two entry modes sharing one shell:
~mime tui SCRIPT.tl --file F~ (step a script locally) and
~mime tui --attach [S]~ (watch a daemon session live). ratatui +
crossterm behind a ~tui~ feature flag so the core stays lean; the
single-threaded ~Rc~ engine fits a TUI event loop as-is.

** TODO Phase 0 — script stepper spike [#B]
~mime tui SCRIPT.tl --file F~: in-process trusted Workspace, three panes
— buffer viewport (rendered from read_region + point, not the gutter'd
~window~ string), program pane (top-level forms, current one
highlighted), report/diff pane (per-step unified diff + reports/log).
SPACE = eval next form, q = quit (discard unless --write).
Prerequisites:
- form-by-form eval: read ONE top-level form at a time from the source
  and eval it against the warm context. If tulisp's reader can't yield
  forms incrementally, that's a tulisp wishlist item (below).
- per-step diff: store snapshot before each form, ~unified_diff~ after
  (snapshots are O(log n) for Quire — cheap even per step).
/Exit: watch a conflict-resolution script land hunk by hunk./

** TODO Phase 1 — playback controls
Auto-play with a speed setting (delay per form), pause/resume, restart
(snapshot at program start; rollback + replay — rehearse semantics make
this free), step-backward via the same snapshots (keep the last N),
finish-and-save vs finish-and-discard. Status bar: point, narrowing,
stale flag, step n/m, buffer name/version.

** TODO Phase 2 — daemon cockpit (--attach)
Watch an AGENT edit live: poll the daemon (the accept loop is
single-threaded and serves one connection at a time, so short poll
connections, not a held-open subscription) with a new read-only
~op_render {session, pos?, lines}~ + the existing op_status; panes for
viewport, per-run diffs as they land, audit-journal tail, session list.
Read-only by design — the cockpit must never be a second writer.
Stretch: a REPL pane that sends forms to the watched session (it IS a
warm single writer, so interleaving with the agent needs a turn-taking
rule or an explicit "take control" toggle).

** TODO Phase 3 — polish
- treesit-driven syntax highlighting of the viewport (the persistent
  parse + node spans are exactly a fontifier's input).
- edit-level animation: step INSIDE a form (watch replace-regexp sweep
  its matches) — needs the store edit-event log, the same
  infrastructure incremental re-parse (~InputEdit~s) wants; build once,
  serve both.
- conflict hunks rendered with side colouring; ~conflict-context~ as a
  pane.
/Decide before an "editor" grows out of this: granular undo (the
checkpoint mechanism is too coarse for interactive editing) and keymaps
(tulisp-dispatched) are the gating engine features./

* tulisp wishlist

** TODO Incremental reader (one form at a time)
~mime tui~'s stepper needs to read a single top-level form from a
source string (returning the form + the rest), instead of parse-all →
eval-all. Check what the parser already exposes before adding API.

** TODO ~format~ conversions ~%x %o %c %e %g~
** TODO Obarray introspection
Enumerate defined symbols / arity / docs, so MCP can advertise the live
vocabulary and tooling can reflect on a session.
** TODO Cooperative cancellation
A ~thread-signal~-style interrupt — unblocks per-program resource limits
above.
** TODO Parity odds & ends
~assq plist-put pop remhash mapc elt~ / vectors.
