#+TITLE: mime-rs — a scriptable, transactional editing engine for AI agents
#+AUTHOR: Sahas Subramanian
#+DATE: 2026-05-30
#+STARTUP: overview
#+OPTIONS: toc:3 num:t

* Vision

~mime-rs~ (reads "mimers") is a headless, *stateful* text-editing engine that an AI
agent drives by submitting small programs. It is the spiritual heir to shsms's
~mime~ (a ChaiScript-scripted buffer editor), rebuilt in Rust on top of [[file:../tulisp][tulisp]]
(an Emacs-Lisp dialect), and re-aimed squarely at agentic use.

The one-line thesis: *today's agent edit tools are stateless and
content-addressed ("replace this exact string"); they cannot express
programmatic, structure-aware, bulk edits, so agents fall back to ad-hoc shell
or Python and reinvent buffer operations badly.* ~mime-rs~ gives the agent a
third option between "brittle single-string edit" and "arbitrary code
execution": a bounded, auditable, *transactional* editing language with warm
buffers that persist across calls and checkpoints it can rewind to.

Pitch in one breath: *run an edit program against warm buffers, see the diff,
keep it or roll back to any checkpoint — over MCP, sandboxed, with no
re-reading of files.*

* Why this, why now

** The gap in current AI editing tools
- /Stateless/: every edit re-reads the file and reproduces exact strings. State
  ("where am I", "what's selected") cannot be carried between operations.
- /Content-addressed only/: "find old_string → new_string" cannot say "for every
  match, do X within its enclosing region", "after the next =</p>=", "between
  point and mark", or "only within this narrowed section".
- /No transaction/: a wrong edit is not atomically revertable. There is no cheap
  "try this whole program; if it's wrong, undo all of it".
- /No structured feedback/: tools return prose or a whole file, not a diff +
  machine-readable signals an agent can branch on.
- /Stale-read races/: the agent's copy of the file drifts from disk; cf. the
  "File has been modified since read" failure mode.

** Lessons banked from `mime` (the predecessor)
Building the ulysses-annotated pipeline on ~mime~ taught us concretely what to
keep and what to fix. ~mime-rs~ exists to keep the wins and kill the cuts.

| From mime                          | Verdict | mime-rs response                                              |
|------------------------------------+---------+--------------------------------------------------------------|
| buffers + multiple cursors         | keep    | first-class buffers + markers (positions that survive edits) |
| ~narrow_to_region~ (bound a slice) | keep    | ~narrow-to-region~ + ~save-restriction~                      |
| ~find_fuzzy~ (ws/case-insensitive) | keep    | ~search-fuzzy~ with configurable normalization               |
| batch model (write a program, run) | keep    | batch is the *primary* mode; daemon adds warm state          |
| ~replace~ is exact-only            | cut     | ~replace-regexp~ with capture groups, count, replace-in-region |
| ~replace(regex(...),...)~ crashes  | cut     | regex is a first-class arg, not a dispatch accident          |
| ~"\xa0"~ literal panics (bad UTF-8) | cut     | never panic on bytes; UTF-8-safe everywhere                  |
| output via ~print~, scrape stdout  | cut     | structured ~(report k v)~ + auto unified diffs               |
| single-shot, cold every run        | cut     | daemon keeps buffers + defs warm across programs             |
| no undo / no dry-run               | cut     | checkpoint tree + transactions + ~rehearse~ (dry-run)        |
| no audit trail                     | cut     | every program + diff logged and replayable                   |

** Why tulisp
- Emacs Lisp + buffers/markers/narrowing is the most battle-tested text-editing
  vocabulary in existence; we adopt its model rather than invent one.
- We *own* the interpreter: editor primitives are plain Rust closures registered
  via ~ctx.defun("name", |..| -> ..)~ (identical to how tulisp ships its own
  builtins, e.g. ~make-hash-table~). Buffers, markers, regions, and checkpoints
  are exposed as opaque tulisp objects (same pattern as tulisp's ~HashTable~).
- tulisp already has a *bytecode compiler* (=src/bytecode=), so hot edit programs
  can be compiled once and replayed cheaply against warm buffers.
- Dogfoods tulisp on a demanding, real workload — which matures tulisp the same
  way the games push does.

* Design principles
1. *Batch-first.* The unit of work is a /program/, not a keystroke. One
   round-trip submits a whole edit program; we never make the agent issue
   cursor-moves one HTTP call at a time. (Round-trips are the thing that kills
   interactive editor protocols for LLMs.)
2. *Warm state.* A long-lived daemon owns buffers, kill-ring, registers, agent
   helper definitions, and the checkpoint tree. Re-reading and re-deriving
   context between programs is the cost we are deleting.
3. *Transactional & reversible.* Every program is atomic by default; any state is
   recoverable from a checkpoint. "Try it, look, keep or rewind" is the core loop.
4. *Structured I/O.* Return diffs and machine-readable reports, not prose or whole
   files. Be token-frugal: diffs and summaries by default, full text on demand.
5. *Auditable & sandboxed.* A bounded command surface with no shell/network/arbitrary
   FS. Granting "mime-rs edit" must be safe in a way that "grant bash" is not.
6. *UTF-8-safe & panic-free.* Malformed input, missing files, and odd bytes are
   errors, never crashes.
7. *Deliberate Emacs subset.* We are re-implementing a chosen /slice/ of Emacs's
   buffer model. The slice is explicit (see §6); we resist becoming Emacs.

* Architecture

#+BEGIN_SRC text
            ┌──────────────────────────────────────────────────────────┐
  clients ─▶│  mimectl (CLI) · MCP server · HTTP/JSON  →  mimed daemon │
            └───────────────┬──────────────────────────────────────────┘
                            │ run_program(session, src, dry_run?)
            ┌───────────────▼──────────────────────────────────────────┐
            │  Session manager:  sessions ⇒ {workspace, ctx, history}  │
            │   - isolation, concurrency, lifecycle, audit log         │
            └───────────────┬──────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────────┐
        ▼                   ▼                       ▼
 ┌─────────────┐   ┌──────────────────┐   ┌────────────────────┐
 │ tulisp ctx  │   │  Buffer store    │   │  Checkpoint tree   │
 │ + editor    │◀─▶│  Quire, markers, │◀─▶│  COW snapshots of  │
 │ builtins    │   │  point/mark/     │   │  the whole         │
 │ (defun'd)   │   │  narrowing       │   │  workspace         │
 └─────────────┘   └──────────────────┘   └────────────────────┘
#+END_SRC

** Components
- *Editor core* — buffers (/Quire/: piece tree over mmapped backing), point, mark, markers, regions, narrowing;
  UTF-8-safe; the actual text data structure and its operations.
- *tulisp editor API* — Rust closures registered on a ~TulispContext~ that expose
  the editor core to programs (§6). Buffers/markers are opaque tulisp objects.
- *Session manager* — maps a session id to a /workspace/ (a set of buffers + kill
  ring + registers), a tulisp context (with warm agent definitions), a
  checkpoint tree, and an audit log. Handles isolation and concurrency.
- *Checkpoint/transaction layer* — copy-on-write snapshots of the whole workspace;
  the undo/redo/branch/rewind machinery (§7).
- *Transport* — the daemon ~mimed~ exposes one control API (HTTP/JSON over a unix
  socket). Three clients speak it: ~mimectl~ (the CLI — what you use instead of
  hand-rolling HTTP), an MCP server (the agent surface), and raw HTTP. All wrap the
  same engine; a ~mimectl~ one-shot can also run the engine embedded, no daemon.
- *Audit/journal* — append-only record of every submitted program, its diff, and
  its resulting checkpoint, for replay and trust.

* The editor core (data model)

Files can be several GB — larger than RAM — so the buffer must *not* hold all the
text. The store is *Quire*: a *persistent piece tree over immutable, memory-mapped
backing* — VS Code's piece tree, made copy-on-write so it doubles as the checkpoint
mechanism. (A /quire/ is a gathering of folded leaves bound into a section of a book;
ours is a document assembled from pieces.)

- *Backing stores* (immutable, shared by every snapshot):
  - /original/ — the file, ~mmap~ed read-only; the OS pages it in/out, never fully
    resident. Behind a trait, so an explicit paged-LRU reader (bounded memory, no
    SIGBUS) can replace ~mmap~.
  - /add buffer/ — append-only, holds *only* inserted text; spills to a temp file if
    it grows large.
- *Buffer* := a *persistent measured B-tree of pieces* + a name + optional file path
  + per-buffer /point/, /mark/, /markers/, /narrowing/ (min,max), dirty and read-only
  flags. A /piece/ is ~(source, start, len)~ — a reference into a backing store, never
  owned text. An unedited GB file is one piece; an edit splits pieces and points a new
  one at the add buffer; original bytes never move.
- *Tree*: ~Arc~ nodes (a snapshot is ~Send + Sync~ → lock-free parallel reads;
  tulisp's own ~Rc~ objects never cross threads), path-copying on edit (O(log n)
  nodes copied); each node carries
  a monoid /summary/ (bytes, chars, lines, + a hook for a marker index) so we seek by
  byte/char/line in O(log n). Same measured persistent B-tree as a rope — only the
  leaf payload changes from owned string to piece reference, so the spine can be our
  own or a vendored ~sum_tree~.
- *Snapshots are ~KB, even for GB files*: a checkpoint clones only the piece-tree
  spine; both backing stores are shared with zero copying. Buffer memory ≈ edits + the
  OS page cache (evictable).
- *Coordinates*: char positions, 1-based, Emacs-style (~point-min~ = 1); byte offsets
  never leak; line/column accessors provided; no op splits a UTF-8 scalar. char/line
  indices over a byte-addressed original need a scan to find boundaries — built lazily
  / in the background / from a persisted sidecar, never blocking the first edit.
- *Markers / anchors*: an anchor = ~(backing-id, byte-offset, bias)~ into an immutable
  store, so it is stable across *every* snapshot for free; resolving it to a line/col
  needs only the target snapshot's tree. (Offset-shifted-on-edit markers also work
  within one timeline; anchors are what survive the checkpoint *tree*.)
- *Non-UTF-8* input (binary, odd logs): rejected at open initially; an explicit byte
  mode can come later.
- *Workspace*: the set of buffers in a session + the shared kill-ring + registers.
  A *checkpoint snapshots the whole workspace*, not a single buffer.

* The tulisp editing API (the scripting surface)

Programs are *Emacs Lisp* (tulisp), *not* mime's ~buffer.method()~ style: an implicit
*current buffer* with *point* and *mark*; edits via ~insert~ / ~delete-region~ /
~replace-match~; search via ~re-search-forward~ + ~match-data~; scoping via
~save-excursion~ / ~save-restriction~ / ~save-match-data~. Names track Emacs so a
model's prior knowledge transfers; only genuinely new operations (mmap-backed open,
checkpoints) take new names. Grouped vocabulary (initial target set):

(Regex /dialect/ is an open decision — Emacs's backslash-heavy syntax vs the ~regex~
crate's PCRE-ish syntax. Leaning PCRE-ish behind Emacs /function names/; see the
tulisp wishlist for the streaming-vs-backreferences tradeoff.)

** Buffers & workspace
| ~(open-file PATH &optional read-only)~     | open/attach a file as a buffer, return it     |
| ~(create-buffer NAME)~ / ~(get-buffer N)~  | scratch buffers                               |
| ~(current-buffer)~ / ~(set-buffer B)~      | the implicit target of edit ops               |
| ~(with-current-buffer B BODY...)~          | run body with B current (restores after)      |
| ~(buffer-list)~ / ~(buffer-name)~ / ~(kill-buffer B)~ | enumerate / introspect / drop      |
| ~(save-buffer)~ / ~(write-file PATH)~      | the *only* way text reaches disk              |
| ~(buffer-modified-p)~ / ~(read-only-p)~    | dirty / writability                           |

** Point & navigation
~point~, ~goto-char~, ~point-min~, ~point-max~, ~forward-char~/~backward-char~,
~forward-line~, ~beginning-of-line~/~end-of-line~, ~beginning-of-buffer~/~end-of-buffer~,
~line-number-at-pos~, ~char-after~/~char-before~, ~looking-at~, ~skip-chars-forward~.

** Mark, region, markers
~set-mark~, ~mark~, ~region-beginning~/~region-end~, ~exchange-point-and-mark~,
~push-mark~/~pop-mark~; ~make-marker~, ~set-marker~, ~marker-position~, ~point-marker~,
~copy-marker~. (Markers are the durable handles agents should prefer.)

** Search (the big upgrade over mime)
| ~(search-forward STR &optional bound)~     | exact forward, sets match data       |
| ~(search-backward STR &optional bound)~    | exact backward                       |
| ~(re-search-forward RE &optional bound)~   | *regex*, with capture groups         |
| ~(re-search-backward RE &optional bound)~  | regex backward                       |
| ~(search-fuzzy STR &optional opts)~        | ws/case-insensitive (mime's superpower) |
| ~(match-string N)~ / ~(match-beginning N)~ / ~(match-end N)~ | capture access      |
| ~(count-matches RE &optional beg end)~     | tally without moving                 |
~search-fuzzy~ options: ~:ignore-case~, ~:collapse-whitespace~, ~:nbsp-is-space~ —
all the normalizations mime baked in implicitly, now explicit and per-call.

** Editing
| ~(insert STR...)~ / ~(insert-before-markers STR)~ | write at point                  |
| ~(delete-char N)~ / ~(delete-region BEG END)~      | remove                          |
| ~(erase-buffer)~                                   | clear                           |
| ~(replace-string FROM TO &optional beg end)~       | exact, *with bounds + count*    |
| ~(replace-regexp RE TO &optional beg end)~         | regex, ~\1~ backrefs, bounds     |
| ~(replace-match TO)~                               | replace the last match          |
| ~(kill-region BEG END)~ / ~(copy-region-as-kill)~ / ~(yank)~ | cut/copy/paste via kill-ring |
| ~(buffer-string)~ / ~(buffer-substring BEG END)~   | read text out                   |

** Narrowing & excursion
~narrow-to-region~, ~widen~, ~save-restriction~ (run body, restore narrowing),
~save-excursion~ (run body, restore point+mark), ~save-mark-and-excursion~.
These let a program operate on a slice and tidy up after itself — the chapter-
bounding trick from the ulysses pipeline, made composable.

** Kill-ring & registers
~kill-ring~ access; ~set-register~/~get-register~ to stash text, positions, or
rectangles by key (handy for an agent juggling several snippets).

** Observability (return data to the agent)
| ~(report KEY VALUE)~ | attach a machine-readable signal to the result (e.g. counts) |
| ~(message FMT ...)~  | human-readable log line                                      |
| ~(echo-region B E)~  | ask the result to include this snippet (on-demand context)   |
~report~ replaces the "grep my stdout for =found:=" antipattern with structured
fields the agent can branch on.

** Transactions & checkpoints (callable from inside a program — see §7)
~(checkpoint LABEL)~, ~(restore-checkpoint ID)~, ~(with-transaction BODY...)~,
~(rollback)~, ~(checkpoint-diff A B)~.

** Structural / syntax-aware editing (later milestone)
Tree-sitter-backed: ~(treesit-search QUERY)~, ~(node-at-point)~, ~(narrow-to-defun)~,
~(replace-node NODE STR)~ — "rename this function", "edit only inside this block".
This is where ~mime-rs~ becomes a serious *code* editor, not just text.

** Warm definitions
A session may preload a tulisp /library/ (agent-defined helper ~defun~s),
evaluated once and persisted across programs. So the agent teaches the editor a
vocabulary ("join-adjacent-links", "wrap-in-tag") and reuses it cheaply.

* Checkpoints, transactions, and time travel   :headline:

This is the feature the user asked for first and the one agents need most.

** Model
- A *checkpoint* is a snapshot of the *entire workspace*: every buffer's text +
  point/mark/markers/narrowing, plus kill-ring and registers.
- Checkpoints form a *tree* (not a stack): restoring an old checkpoint and editing
  forward creates a branch (undo-tree semantics), so the agent never loses a
  state by exploring an alternative.
- Snapshots are *cheap* even for multi-GB files: the immutable backing stores
  (mmapped original + append-only add buffer) are shared, so a checkpoint copies only
  the piece-tree spine (~KB) — O(1) in *file* size, paid lazily on later edits.

** Operations
| ~(checkpoint "label")~        | create a named checkpoint, return its id                 |
| ~(restore-checkpoint id)~     | rewind the whole workspace to it (current branch updates) |
| ~(list-checkpoints)~          | the tree: id, label, parent, time, buffers touched       |
| ~(checkpoint-diff A B)~       | unified diff of the workspace between two checkpoints     |
| ~(current-checkpoint)~        | where we are                                             |
| ~(forget-checkpoint id)~      | GC an unwanted branch                                    |

** Automatic transaction per program
Every submitted program is implicitly wrapped:
1. auto-checkpoint the workspace *before* the program runs;
2. evaluate the program;
3. on *success* → keep the changes, the pre-checkpoint remains as the rewind point;
   on *tulisp error / explicit ~(rollback)~* → atomically restore the
   pre-checkpoint; the result reports ~rolled_back_to~. No partial mutation leaks.
~(with-transaction ...)~ exposes the same all-or-nothing guarantee at sub-program
granularity for risky inner steps.

** Dry-run / rehearse
~rehearse~ mode (CLI ~--rehearse~, MCP ~dry_run: true~) runs the program, computes the
resulting diffs and reports, then *always rolls back* and returns the diff
without committing. "Show me what this program /would/ do." This would have saved
dozens of full epub rebuilds in the ulysses work.

** Why this matters for agents
Agents are wrong often. Cheap, named, *branching* rewind turns "I made a mistake,
now reconstruct the file" into ~(restore-checkpoint "before-refactor")~. Combined
with ~rehearse~, the agent's loop becomes: rehearse → inspect diff → commit or
adjust → checkpoint. Mistakes cost one call, not a salvage operation.

* Structured I/O for agents

** Result of a program run (JSON)
#+BEGIN_SRC json
{
  "ok": true,
  "session": "s_8f3",
  "checkpoint": "auto-42",          // pre-run rewind point; null in rehearse
  "committed": true,                // false for rehearse
  "buffers": [
    { "name": "4300-h.htm", "dirty": true,
      "diff": "@@ -120,6 +120,7 @@ ...unified...",
      "diff_truncated": false,
      "point": 10293, "mark": null, "narrowed": false,
      "len_before": 4300120, "len_after": 4300140 }
  ],
  "reports": { "found": 4432, "distinct": 1195 },   // from (report ...)
  "log": ["narrowed to chapter 12", "..."],          // from (message ...)
  "elapsed_ms": 137
}
#+END_SRC

** Error result
#+BEGIN_SRC json
{
  "ok": false,
  "error": { "kind": "tulisp-error",
             "message": "re-search-forward: bound exceeds point-max",
             "form": "(re-search-forward \"</a>\" 999999)",
             "buffer": "index.php-cyclops.htm", "point": 88123,
             "backtrace": ["fix-cyclops", "run"] },
  "rolled_back_to": "auto-41",
  "committed": false
}
#+END_SRC

** Token discipline
- Default to *diffs and summaries*, never whole buffers.
- Cap diff size; when exceeded, return a stat summary + a handle to fetch ranges.
- The agent pulls full text only via explicit ~read_region~ / ~(echo-region ...)~.
- Positions, match data, and counts are returned numerically so the agent can act
  without re-reading.

* Daemon, sessions, and concurrency

- *Daemon* (~mimed~ — "mimed", as in mime + d): long-lived, owns all sessions and
  serves the control API. Clients reach it via ~mimectl~, MCP, or raw HTTP. A
  ~mimectl~ one-shot can also run the engine in-process without a daemon.
- *Session* = isolated workspace + tulisp context + checkpoint tree + audit log.
  Mapped 1:1 to an agent conversation by convention. Created explicitly; idle-
  evicted on a TTL; buffers in a session never leak to another.
- *Concurrency*: programs within a session are serialized (a session is a single
  writer — simplest correct model; edits are inherently ordered). Different
  sessions run in parallel. Per-buffer write locks guard shared on-disk files
  touched by multiple sessions.
- *Durability*: buffers are in-memory; text reaches disk only via ~save-buffer~ /
  ~write-file~. Optional WAL journaling of programs lets a crashed daemon replay
  unsaved sessions. Checkpoints are in-memory by default, with an opt-in to
  persist the tree to disk for cross-restart time travel.
- *Lifecycle/limits*: max buffers, max total bytes, max checkpoints per session,
  eviction policy (named checkpoints pinned; anonymous GC'd beyond N).

* Transport: one API (~mimed~), three clients

The daemon ~mimed~ serves a single control API; ~mimectl~, MCP, and raw HTTP are
three front-ends to it. You should rarely hand-write HTTP — reach for ~mimectl~.

** mimectl (the CLI — instead of direct HTTP)
A thin, scriptable client over the daemon API (systemctl/kubectl-style verbs):
| ~mimectl run PROG.tl [--file F] [--rehearse]~       | submit a program (dry-run with --rehearse) |
| ~mimectl open FILE [--read-only]~                   | attach a file to the session            |
| ~mimectl ls~ / ~mimectl status~                     | buffers, dirty flags, current checkpoint |
| ~mimectl search BUF PAT [--regex\vert--fuzzy]~      | run a search, print matches             |
| ~mimectl checkpoint [LABEL]~ / ~mimectl checkpoints~ | create / list the checkpoint tree       |
| ~mimectl restore ID~ / ~mimectl diff A B~           | rewind / show a checkpoint diff          |
| ~mimectl save BUF~ / ~mimectl write BUF PATH~       | persist to disk                          |
| ~mimectl repl~                                      | interactive tulisp against the session   |
~mimectl~ resolves its session from ~$MIME_SESSION~ or ~--session~; with ~--local~ it
runs the engine embedded (no ~mimed~ needed) for one-shots.

** MCP tools (the agent-facing surface)
High-level conveniences, with ~run_program~ as the power-user escape hatch:
| ~open_file(path, read_only?)~                       | attach a file                     |
| ~run_program(program, session?, dry_run?)~          | eval a tulisp edit program        |
| ~search(buffer, pattern, mode)~                     | exact/regex/fuzzy, returns matches |
| ~read_region(buffer, start, end \vert around_point)~ | pull text on demand               |
| ~checkpoint(label)~ / ~list_checkpoints~ / ~restore_checkpoint(id)~ / ~diff(a,b)~ | time travel |
| ~save_buffer(buffer)~ / ~write_file(buffer, path)~  | persist                           |
| ~session_status()~                                  | buffers, dirty flags, current checkpoint |
MCP gives tool schemas, permissioning, and discovery for free — the right
integration surface for agents. HTTP/JSON mirrors these for non-MCP clients.

** HTTP/JSON (the wire ~mimectl~ and MCP ride on)
The raw control API, served by ~mimed~ over a unix socket (TCP optional):
~POST /sessions~, ~POST /sessions/:id/run~, ~GET /sessions/:id/buffers~,
~POST /sessions/:id/checkpoints~, ~POST /sessions/:id/restore~,
~GET /sessions/:id/diff?a=..&b=..~. Sync by default; long programs may return a job
id + ~GET /jobs/:id~. ~mimectl~ is just a friendly skin over these.

* Safety, sandboxing, permissioning

- *Bounded vocabulary*: tulisp programs see only the editor builtins + pure lisp.
  No shell, no process spawn, no network, no arbitrary FS. ~open-file~/~write-file~
  are restricted to configured workspace roots (path allowlist, symlink-checked).
- *Read-only buffers*: reference material can be attached unwritable.
- *Resource limits*: per-program wall-clock timeout, memory cap, max buffer size,
  max output size; eval is interruptible (cooperative cancellation in the
  bytecode interpreter).
- *Determinism*: no ambient time/randomness except via explicit, logged builtins.
- *Auditability*: every program source + its diff + resulting checkpoint is
  journaled. You can answer "what did the agent change, and why" and *replay* it.
- *The pitch*: this is why "let the agent run mime-rs" is safe where "let the agent
  run bash" is not — same editing power, none of the blast radius, full rewind.

* Performance

- *The file is never fully resident.* Quire — piece tree over mmapped (or paged-LRU)
  backing — touches O(log n) pieces and the OS page cache when editing or snapshotting
  a multi-GB file, not the bytes.
- *Checkpoints are O(spine), ~KB*, at any file size — immutable backing is shared
  across all snapshots; only the piece-tree path is copied.
- *Parallelism*: immutable snapshots are ~Send + Sync~, so reads (search / count /
  index) are lock-free parallel and map-shaped writes (e.g. ~replace-all~) parallelize
  too — see *Concurrency & parallelism* below.
- *Indexing is the costly op, not editing.* char/line summaries need one scan of the
  original to find boundaries — done lazily / in the background / from a persisted
  sidecar, so it never blocks the first edit.
- *Search* streams a cursor across pieces (a ~regex-automata~ incremental DFA, so
  matches can cross piece/backing boundaries); the common all-original case scans the
  ~mmap~ directly (~memchr~ / ~regex::bytes~) and maps offsets back.
- *Saving is O(file size)* by nature — stream every piece to a new file + atomic
  rename; a tiny edit to a huge file still rewrites it. Budgeted, not hidden.
- No full re-serialization per op (mime re-walked a 43 MB buffer repeatedly); track
  dirty spans, diff only changed ranges, and *never materialize the whole buffer* —
  the API stays ranged/streamed (mandatory at GB scale, not just token-thrift).
- Compile hot programs via tulisp's bytecode path; cache compiled programs per
  session keyed by source hash.
- *Targets*: open + edit a multi-GB file at flat memory (≈ edits + page cache);
  checkpoint create/restore ~µs–ms at any size; typical agent program sub-second; the
  ulysses-annotated transform (≈43 MB) as a standing perf+correctness benchmark.

* Concurrency & parallelism

One decision (immutable structural sharing) buys two payoffs: cheap checkpoints *and*
lock-free parallel reads. ~Quire~ snapshots are ~Arc~ / ~Send + Sync~ and the mmap is
read-only and shared. Parallelism has two levels — *across sessions* (many agents,
coarse) and *inside a heavy builtin* (fine-grained fan-out) — and the tulisp program
never sees a thread.

** Invariants
- *Single writer per session.* Programs in a session are serialized; an edit builds a
  new immutable version and installs it atomically. Different sessions run in parallel.
- *Parallelism lives inside Rust builtins.* A builtin may fan out across cores and join
  before returning; the interpreter stays single-threaded. Only ~Arc~ snapshots cross
  threads — tulisp's own ~Rc~ objects never do, so we need no ~sync~ feature.
- *Deterministic results.* Parallel work is merged in document order, so output is
  identical regardless of thread count — required for ~rehearse~, audit, and replay.
- *Cooperative cancellation.* Workers honor the per-program timeout / interrupt.

** Reads — parallel and lock-free, always
search / count / index / anchor-resolve over an immutable snapshot split into ranges,
fanned across a work-stealing pool above a size threshold, merged in order. The one
O(file) op — the char/line index — parallelizes as ~memchr~ newline-count per chunk +
prefix sum. Scanning a multi-GB file uses every core.

** Writes — the *map-shaped* subset (stateless per match)
Safe exactly when each match's replacement is an independent, pure function of the
match (a /map/) — not of a running counter, point, or earlier matches (a /fold/).
Find-all yields disjoint matches and replacements are not re-scanned, so independent
regions rebuild in parallel and ~concat~ with no race or cascade.

| primitive                                            | shape                      | parallel?                                |
|------------------------------------------------------+----------------------------+------------------------------------------|
| ~(replace-all REGEX TEMPLATE)~                       | pure data                  | *always* (Rust; no Lisp per match)       |
| ~(map-matches REGEX PURE-FN)~                        | map, per-match fn          | iff PURE-FN is pure (N tulisp instances) |
| ~(while (re-search-forward …) … (replace-match …))~  | fold (threads point+state) | no — sequential by construction          |

~replace-all~ with a backref template is pure data with no Lisp per match — the common
case and the *first* parallel write to ship. ~map-matches~ with a pure per-match Lisp
function is the principled ceiling but needs tulisp run on N workers (gated on purity);
a later step. The general loop is a sequential fold — but still gets *parallel search +
sequential apply*. So parallelism is a property of *which primitive you reach for*
(like ~par_iter().map()~ vs a ~for~ loop), not something the agent reasons about.

(mime-rs may depend on ~rayon~, ~memmap2~, ~memchr~, ~regex~/~regex-automata~; tulisp's
zero-dependency rule is tulisp's, not ours.)

* Migrating from mime

~mime-rs~ is *not* source-compatible (mime is ChaiScript; mime-rs is tulisp), but
the model maps almost 1:1, and the port is cleaner because the rough edges are
gone. Vocabulary map:

| mime (ChaiScript)            | mime-rs (tulisp)                          |
|------------------------------+-------------------------------------------|
| ~buffer(path)~               | ~(open-file path)~                        |
| ~find~ / ~rfind~             | ~search-forward~ / ~search-backward~      |
| ~find_fuzzy~                 | ~search-fuzzy~                            |
| ~find(regex(...))~           | ~re-search-forward~ (regex is first-class) |
| ~set_mark~                   | ~set-mark~ / ~point-marker~               |
| ~goto_pos~ / ~get_pos~       | ~goto-char~ / ~point~                     |
| ~narrow_to_region~ / ~widen~ | ~narrow-to-region~ / ~widen~              |
| ~copy~ / ~cut~ / ~paste~     | ~buffer-substring~ / ~kill-region~ / ~insert~ |
| ~replace(a,b)~ (exact only)  | ~replace-string~ / ~replace-regexp~ (+bounds,count) |
| ~new_cursor~ / ~use_cursor~  | markers + ~with-current-buffer~ / ~save-excursion~ |
| ~print~                      | ~report~ / ~message~                      |
| (none)                       | ~checkpoint~ / ~with-transaction~ / ~rehearse~ |

*Case study / integration test*: port ~ulysses-annotated/scripts/add-anno.mime~ to
~mime-rs~ and reproduce the annotated HTML. It exercises every primitive
(multi-buffer, narrowing, fuzzy + regex search, region replace, structured
counts) and gives an immediate real-world acceptance test.

* Testing & validation
- *Unit*: each builtin, incl. UTF-8 edge cases (NBSP, combining marks, lone
  surrogates rejected) — the ~\xa0~ crash becomes a permanent regression test.
- *Property*: ∀ program P and checkpoint C, ~restore(C)~ after P == state at C;
  ~undo~∘~edit~ == identity; markers track edits correctly.
- *Golden*: (program + input buffer) ⇒ expected unified diff.
- *Fuzz*: random edit programs never panic and never corrupt UTF-8.
- *Integration*: the ulysses port (above); concurrent-session isolation;
  daemon crash/replay from the WAL.
- *Bench*: the 43 MB transform; open/edit a multi-GB file at flat memory; checkpoint
  create/restore at any size.

* Roadmap / milestones

/Status: M0 ✓, M1 ✓ (persistent B-tree, O(log n) seeks, O(1) snapshots), M2 ✓
(checkpoints + transactions + cheap snapshots), M3 ✓ (~mimed~ daemon + warm sessions),
M4 ✓ (~mime-mcp~ — MCP server, 12 tools over JSON-RPC/stdio). 107 tests, clippy clean;
the agent-facing surface works end to end. M5 ✓ (safety/audit), rehearse ✓, M7 scaffold ✓ (tree-sitter Markdown). M6/M8 remain./

** DONE M0 — Editor core + tulisp API (replace a mime batch) [#A]
   In-memory ~TextStore~ (bring-up + differential-test *oracle* only — not the real
   buffer); point/mark/markers/narrowing; the §6 vocabulary (exact + regex + fuzzy
   search, region/regexp replace, insert/delete, narrowing, excursion);
   ~mimectl run prog.tl --file x --local~ one-shot; UTF-8-safe; structured result with
   auto diff + ~report~. /Exit: re-run a real mime transform./
   /Done: ~60 Emacs-Lisp builtins (motion, mark/region, search incl. ~search-fuzzy~,
   ~replace-string~/~replace-regexp~, kill-ring, narrowing, ~save-excursion~/
   ~save-restriction~) + the RE2 string library; ~mimectl run --local~ opens files via
   Quire; 46 tests./
** DONE M1 — Quire: the piece-tree ~TextStore~ (multi-GB) [#A]
   *Quire*, the real buffer — a persistent measured B-tree of pieces over a ~Backing~ trait
   (mmap + paged-LRU) + append-only add buffer; ~Arc~ nodes (snapshots ~Send + Sync~);
   lazy/background char-line index; anchors. Becomes the default ~TextStore~; the
   in-memory impl stays as the differential-test oracle. /Exit: open + edit a
   multi-GB file at flat memory; oracle and piece tree agree under property tests./
   /Done: first cut is a piece-table (~Vec<Piece>~ over the mmapped original + add
   buffer), all 30 ~TextStore~ methods, differential-tested ~24k random ops vs the
   oracle. The ~Vec~ → persistent measured B-tree upgrade is *done*: ~Arc~-node
   persistent tree, O(log n) seeks via ~{bytes,chars,lines}~ summaries, and
   ~Quire::snapshot~ in O(1) with shared backings — the basis for ~KB checkpoints./
** DONE M2 — Checkpoints, transactions, dry-run [#A]
   Workspace COW snapshots (clone the piece-tree spine, ~KB); checkpoint tree;
   auto-transaction per program; ~with-transaction~; ~rehearse~; ~checkpoint-diff~.
   /Exit: rewind + dry-run on multi-buffer edits; property tests green./
   /Done: ~checkpoint~ / ~restore-checkpoint~ / ~with-transaction~ (full-text snapshot
   MVP in ~Session.checkpoints~). Pending: ~KB structural-sharing snapshots (needs the
   Quire B-tree + a ~snapshot~ trait method), ~rehearse~ dry-run (daemon-level), and
   ~checkpoint-diff~./
** DONE M3 — Daemon (~mimed~), ~mimectl~, sessions, warm state [#A]
   ~mimed~ + ~mimectl~ client; session isolation + lifecycle + limits; warm buffers
   and warm defs across programs; control API (HTTP/JSON over a unix socket); WAL
   journaling. /Exit: two sessions edit in parallel; buffers persist across programs./
   /Done: ~Workspace~ holds a warm ~TulispContext~ + ~Session~ across programs; ~mimed~
   serves a JSON-lines control API over a unix socket (~open~/~run~/~status~/~save~/
   ~close~/~ping~) with a session map; ~mimectl~ gained daemon verbs (~--local~ one-shot
   unchanged). Single-threaded loop (the ~Rc~ ctx is ~!Send~); the
   ~Mutex<HashMap<Workspace>>~ seam is kept for future per-session sharding + WAL./
** DONE M4 — MCP server + agent ergonomics [#A]
   MCP tools (§11); structured results tuned for token budget; ~read_region~
   on-demand context; permission-friendly tool schemas. /Exit: an agent edits a repo
   end-to-end over MCP with no file re-reads./
   /Done: ~mime-mcp~ — JSON-RPC 2.0 over stdio, embeds the engine, 10 tools
   (~open_file~/~open_text~/~run_program~/~read_region~/~search~/~checkpoint~/
   ~restore_checkpoint~/~list_checkpoints~/~save_buffer~/~session_status~) with JSON
   Schemas; warm per-session ~Workspace~ map; full handshake. FS tools carry a
   ~// TODO: path allowlist~ (M5)./
** DONE M5 — Safety & audit [#B]
   Path allowlists, read-only buffers, resource limits, interruptible eval,
   append-only audit journal + replay. /Exit: safe to grant to an autonomous agent./
** TODO M6 — Parallelism & performance [#B]
   Lock-free parallel reads over immutable snapshots (search / count / index /
   anchor-resolve fanned across cores); parallel bulk replace (divide-and-conquer
   rebuild + ~concat~); parallel initial line index (~memchr~ + prefix sum);
   rebase-on-save compaction (after a save, re-mmap the new file, collapse to one ~Original~ piece, release the pinned pre-save inode + reset the add buffer); bytecode-compile hot programs; dirty-span diffs; benchmark suite. /Exit: search +
   global-replace a multi-GB file scale across cores; ulysses bench beats mime./
** DOING M7 — Structural / syntax-aware editing [#C] (scaffold landed)
   Tree-sitter integration; node search/replace; narrow-to-defun. /Exit: AST-scoped
   refactors on real code./
** TODO M8 — Polish [#C]
   ~mimectl repl~; mime→mime-rs porting guide; docs; example library of helpers;
   ~cargo~ workspace + crate publish.

* tulisp substrate — what mime-rs relies on

Read against tulisp 0.28's *tests, doc-comments, and code*. (The loose ~docs/*.md~ and
~todo.org~ are untracked working notes and can be stale — verify in ~tests/~ + source,
not there.) What we build on, and the real constraints:

** Registration (how the editor API attaches)
- Primitives are Rust closures via ~ctx.defun("name", |..| -> ..)~. ~&optional~ →
  ~Option<T>~, ~&rest~ → ~Rest<T>~, fallible → ~Result<T, Error>~, interpreter access →
  ~&mut TulispContext~ as the first param. Arg/return types: ~i64 f64 bool String Number
  Vec<T> TulispObject~.
- ~Quire~ buffers, markers, anchors, checkpoints are *opaque values* via
  ~TulispConvertible~ / ~Shared<dyn TulispAny>~ (Clone + Display), like tulisp's hash
  table. Keyword-heavy builtins (~search-fuzzy :ignore-case …~) take ~Plist<T>~ +
  ~AsPlist!~.
- *Real cap*: ~defun~ takes a small fixed number of params (5; 10 with the
  ~big_functions~ feature, confirmed in ~Cargo.toml~). → enable ~big_functions~ and
  prefer ~Plist~ for option-heavy primitives.

** Language we lean on (present + tested)
- Control / bind: ~if cond when unless and or while dolist dotimes let let* setq progn
  prog1 prog2 catch throw condition-case~; ~lambda funcall apply~; ~defun defmacro
  defvar~; backquote/unquote; threading macros (~-> ->> when-let if-let while-let~).
- *General TCO — self and mutual — on both backends* (pinned by
  ~test_mutual_tail_recursion_is_tco~ at depth 200k via ~Instruction::TailCall~ + an
  arity pre-pass): recurse or loop freely.
- Lexical binding ~t~: closures capture lexically (a per-match transform fn is
  well-behaved).
- Lists / seqs: ~car cdr cons setcar setcdr append nth nthcdr last length reverse member
  assoc alist-get plist-get push~; ~mapcar mapconcat seq-map seq-filter seq-reduce
  seq-find sort~.
- Strings / misc: ~concat make-string prin1-to-string princ~; ~format~ — but only
  ~%s %S %d %f~ (with field widths + ~-~/~0~ flags; ~%05d~ / ~%-10s~ are tested);
  ~intern gensym~; hash tables (~make-hash-table gethash puthash~); the full numeric
  tower.

(Heavy per-element bulk work lives in Rust builtins so it can parallelize — see
§Concurrency — with tulisp orchestrating. A design choice, not a tulisp limit.)

* tulisp wishlist — build in tulisp for mime-rs

Emacs-Lisp text plumbing tulisp doesn't have yet (confirmed by absence in ~src/~),
roughly in priority order:

1. *Regex + match-data* — the big one; tulisp has *none*. ~string-match~,
   ~match-beginning~ / ~match-end~ / ~match-string~, ~save-match-data~,
   ~replace-regexp-in-string~ (string + *function* REP — the parallelizable shape),
   ~regexp-quote~. The buffer ~re-search-forward~ / ~looking-at~ / ~replace-match~ are
   mime-rs's to add but share this substrate.
   /Dialect tradeoff:/ the ~regex~ / ~regex-automata~ crate (RE2: linear-time,
   *streamable across pieces*, parallel-friendly) is the right engine for GB files — but
   RE2 has no in-pattern backreferences or look-around, which Emacs regex has (via
   backtracking). Recommend RE2 syntax behind Emacs /function names/; replacement-side
   backrefs (~\1~) are fine. Decide consciously.
2. *String library* — ~substring split-string string-search string-replace string-trim
   string-prefix-p string-suffix-p number-to-string string-to-number upcase downcase
   capitalize char-to-string string-to-char~.
3. *~format~ conversions* — has ~%s %S %d %f~ (+ widths/flags); add ~%x %o %c %e %g~.
4. *~unwind-protect~* — *DONE*, merged into tulisp: the special form for guaranteed
   cleanup (temp files, locks) around fallible edits.
5. *Obarray introspection* — enumerate defined symbols / arity / docs, so MCP can
   advertise the live vocabulary and tooling can reflect on a session.
6. *Cooperative cancellation* — a ~thread-signal~-style interrupt so mime-rs can enforce
   per-program timeouts instead of letting a body run to completion.
7. *Parity odds & ends* — ~assq plist-put pop remhash mapc elt~ / vectors.

Items 1–2 are the critical path (Emacs editing is regex-and-string-shaped); 3–7 are
incremental. Several already sit on tulisp's own roadmap, so mime-rs is a forcing
function for both.

* Dogfooding friction log

Findings from editing mime-rs *through mime-rs* (the agent's-eye view); appended as we go.

- Allowlist gotcha (M5): the first cross-repo edit failed until ~MIME_ROOTS~ was set to
  touch a file outside cwd. Correct + a clear error, but an agent hits it immediately;
  the daemon/MCP should advertise the allowed roots up front.
- Viewing was indirect: ~(message (window ...))~ then digging the string out of the JSON
  ~log~ is clunky; ~view~ wants to be a first-class MCP tool returning the text. [done: the view tool]
- No durable markers yet: multiple viewports work via ~(window N pos)~ + vars, but a saved
  position does not survive edits; Emacs markers on Quire anchors are the fix. [done: make-marker/point-marker/copy-marker/set-marker/marker-position/markerp, and goto-char takes a marker]
- Inserting big literal blocks is awkward: tulisp-string escaping of newlines is
  error-prone (this very section was a long escaped ~insert~); want an ~insert_text~ MCP
  tool that takes the text as a JSON string, no tulisp escaping. [done: insert_text]
- Structural code edits are hard: adding the ~view~ MCP tool meant three separate
  sites (a handler fn, a dispatch arm, a schema entry). Search/replace is the wrong
  instrument for "add an arm to this match" / "add a field to this struct", so I used a
  plain editor for those. This is the argument for AST-aware ops (M7 tree-sitter):
  mime-rs shines at prose and find/replace transforms, less so at structural code.
- MCP path is live: ~open_file → search/view → insert_text/run_program → save_buffer~ as
  native tools — this very bullet was written through ~insert_text~ (no Lisp/JSON escaping).
  Onboarding friction: ~claude mcp add~ defaults to *local* scope bound to the cwd's project,
  so registering from ~/vagrant/mime-rs~ hid the server from the ~/vagrant~ session and the
  first resume saw no tools; ~-s user~ (or adding from the session root) fixes it. An agent
  can't self-register reliably without knowing this — the installer should pick scope on purpose.
- Markers shipped: ~make-marker/point-marker/copy-marker/set-marker/marker-position/markerp~, and
  ~goto-char~ takes a marker. Demoed two durable cursors over Section B/C that survived a
  top-of-buffer insert (the multi-viewport feature). Implemented as a per-store registry that
  auto-adjusts on insert/delete/replace; ~TulispConvertible~ makes ~Marker~ a first-class value.
- Warm server is frozen at spawn: new *builtins* (like new *tools*) do not appear in a running
  mime-mcp/mimed until it restarts — ~point-marker~ was void through MCP right after I added it,
  though tests and ~mimectl --local~ (fresh process per call) had it. So ~mimectl --local~ is the
  fast dev-iteration path; the MCP/daemon path needs a ~claude --resume~ to pick up new vocabulary.
- BUG (save-safe, intermittent): editing a large real file through the *warm* MCP session,
  ~buffer-substring~/~read_region~ sometimes return the wrong window (mid-document content as the
  apparent tail) after several edits with snapshots between them. The spine is fine —
  ~full_text~/~save_buffer~ wrote correct content every time and ~point-max~ matched the real
  length — so no data loss, but ~view~/~read_region~ can mislead. Not reproduced on synthetic text
  yet (six variations pass); RESOLVED — FIXED. The root Summary is correct *by construction* (every node constructor recomputes it) and ~collect_range~ matches the oracle across 64k+ snapshot-bracketed, mmap-backed, multibyte ops — the core is provably sound; the *B-tree itself was correct all along*. The actual fault: ~save_buffer~ overwrote the same file ~Quire~ had mmapped as its immutable original, mutating those bytes under the live pieces, so mmap-backed reads (~collect_range~ behind ~buffer-substring~/~read_region~/search) returned shifted content while ~full_text~/save (cache) stayed correct. FIX: saves now go through ~safety::write_atomic~ (temp file + atomic rename), leaving the mmapped inode intact. Regression-tested end to end (open → edit → in-place save → reads stay byte-correct; the guard fails with a plain in-place write). Workaround: a
  fresh ~open_file~ (or ~mimectl --local~) reads correctly.

* Open questions & risks (decide deliberately)
1. *Why not ~emacsclient~ + elisp?* It already has buffers/markers/narrowing/regexp
   and a server. Our answer: lean single-binary embeddability, a shaped/bounded
   primitive set, JSON-native results, transactions-for-agents, and a sandbox we
   control. Valid — but we are choosing to reimplement a /slice/ of Emacs; name
   the slice and stop there.
2. *Why not sandboxed Python + a buffer lib?* The differentiators must be: warm
   cross-call state, an auditable bounded surface, and first-class transactions.
   If those don't feel decisive, the niche is thin — pressure-test this early.
3. *tulisp maturity/perf*: is the interpreter (even bytecode-compiled) fast enough
   over multi-GB buffers? This is a forcing function for tulisp; budget for it.
4. *Checkpoint memory & persistence*: GC policy for branches; do checkpoints
   survive a daemon restart? (WAL replay vs. persisted tree.)
5. *Multi-session edits to the same on-disk file*: conflict/lock policy; last-
   writer-wins vs. detect-and-refuse.
6. *Coordinate model*: confirmed char-based, 1-based, byte-safe — but line-oriented
   tools may want line/col first-class too.
7. *Diff granularity vs. token budget*: where to truncate, and the fetch-on-demand
   contract.
8. *Scope creep*: the gravitational pull toward "reimplement Emacs". Guard with the
   principle in §3.7 and the explicit milestone exits.

* Appendix A — end-to-end example

A program that, within one chapter slice, joins adjacent same-note links, counts
them, and checkpoints — run in ~rehearse~ first to preview the diff.

#+BEGIN_SRC tulisp
(with-current-buffer (open-file "index.php-cyclops.htm")
  (let ((b (point-min)) (e (point-max)) (joined 0))
    (save-restriction
      (narrow-to-region
        (search-forward "[ 12 ]") (search-forward "[ 13 ]"))
      (goto-char (point-min))
      ;; merge </a><a ...same href...> runs
      (while (re-search-forward "</a><a href=\"\\([^\"]+\\)\"[^>]*>" nil)
        (when (same-note-p (match-string 1))      ; a warm helper def
          (replace-match "")
          (setq joined (1+ joined)))))
    (report "joined" joined)
    (checkpoint "cyclops-links-joined")))
#+END_SRC

The result returns the unified diff for the buffer, ~"reports": {"joined": N}~, and
the new checkpoint id — committed only if not run as ~rehearse~.

* Appendix B — naming & lineage
- Project / engine crate: ~mime-rs~ (reads "mimers"; the ~-rs~ Rust suffix completes
  the word).
- Daemon binary: ~mimed~ ("mimed" — mime + d, and the past tense fits: it has
  already performed your edits).
- CLI binary: ~mimectl~ (systemctl/kubectl convention — the control client you use
  instead of raw HTTP).
- Lineage: shsms's ~mime~ → ~mime-rs~. Same buffers-and-cursors soul; new powers
  (transactions, structured I/O, MCP) and none of the cuts.
- Tagline candidate: /"an amanuensis for your agents — it takes dictation, keeps
  every draft, and never loses your place."/
